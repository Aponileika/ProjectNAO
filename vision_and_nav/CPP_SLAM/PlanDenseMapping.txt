This is the overarching plan for dense mapping.
- 16 sep, read stereo camera feed from dataset, with calibration parameters.
- 18 sep, produce dense local camera per camera map with stereo.
- 22 sep, voxel representation of map.
- 24 sep, rolling dense map. !!! This implies sharing keyframe pose with dense mapping.
- 26 sep, refactor sl slam, split tracking and localmapping into their own files

TODO:
Dense has been implemented, as a first test add a visualization of the disparity output for each keyframe and
run the code live, so each time a keyframe is inserted the disparity viz should update.
My idea here is that tracking enqueues both the dense queue and the keyframe queue. 
