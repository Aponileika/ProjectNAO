What to do next:
    - Finish local mapping pipeline, triangulation with epipolar constraint (search along epipolar line) and features! keyframe culling
    - Implement keyframe removal functionality and mappoint removal functionality
    - Implement initialization, see paper that uses historical data
    - Implement threading, use same structure as orb, tracking <-> local mapping <-> global mapping threads. std::jthread / thread? 
      Think about how to mutex or not to, or copy? Make a decision dont just autopilot into a too restrictive mutex situation.
